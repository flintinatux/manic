{
  "name": "manic",
  "version": "0.0.0",
  "description": "game engine",
  "author": "flintinatux",
  "license": "ISC",
  "main": "index.js",
  "scripts": {
    "build": "browserify index.js --standalone Manic > dist/manic.js",
    "clean": "rm -rf dist",
    "postbuild": "ccjs dist/manic.js > dist/manic.min.js",
    "prebuild": "npm run clean && mkdir -p dist",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "eventemitter2": "^1.0.0",
    "lodash": "^4.6.1"
  },
  "devDependencies": {
    "acorn": "^3.0.4",
    "browserify": "^13.0.0",
    "closurecompiler": "^1.5.2"
  }
}
